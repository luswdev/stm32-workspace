name: Docker Image CI in Windows

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Build the Docker Image
        run: docker build --network=host -t stm32-workspace . -f docker/toolchain.dockerfile

      - name: Test Build STM32 Flash Image
        run: docker run -v $(pwd):/opt/project -i --rm --hostname stm32-workspace --workdir /opt/project stm32-workspace:latest ./build.sh

